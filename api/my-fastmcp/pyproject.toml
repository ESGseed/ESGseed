[project]
name = "gateway"
version = "0.1.0"
description = "API Gateway service with FastAPI"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.104.1",
    "openai>=2.8.1",
    "python-dotenv>=1.2.1",
    "uvicorn[standard]>=0.24.0",
]
